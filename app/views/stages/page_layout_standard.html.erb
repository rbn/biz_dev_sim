<h2><%= @stage.label %></h2>

<%= image_tag @stage.featured_image_url, class: :featured %>

<%= raw @stage.featured_text %>

<div class="back_to_board hidden">
  <input id="back_to_board" type="button" value="back to the board" />
</div>
